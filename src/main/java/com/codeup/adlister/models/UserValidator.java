package com.codeup.adlister.models;

// Validator class
public class UserValidator {
    private boolean validationConditions;

    public boolean validate(User user) {
        // Perform validation checks
        //user is not valid
        return validationConditions; //user is valid
    }
}
